# Server-side only. The API is reached through TanStack Start server functions.
OINK_API_URL=
OINK_API_FALLBACK_URL=

# Public values are bundled into the frontend.
VITE_APP_URL=http://localhost:3000
VITE_SOLANA_NETWORK=devnet
VITE_SOLANA_EXPLORER=https://solscan.io
VITE_PRIVY_APP_ID=

# Server-side only. Same value as the backend's OINK_PROXY_SECRET.
OINK_PROXY_SECRET=
